export const RE_EMAIL =
  // eslint-disable-next-line unicorn/better-regex
  /^(([^\s"(),.:;<>@[\\\]]+(\.[^\s"(),.:;<>@[\\\]]+)*)|(".+"))@((\[(?:\d{1,3}\.){3}\d{1,3}\])|(([\da-z-]+\.)+[a-z]{2,}))$/i;
